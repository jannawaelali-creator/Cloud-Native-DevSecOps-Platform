apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: devops-project
spec:
  type: NodePort
  ports:
  - port: 80          
    targetPort: 80 
    nodePort: 30007   
  selector:
    app: frontend

