apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: devops-project
data:
  API_PORT: "3000"
  NODE_ENV: "production"
  DB_HOST: "postgres-service.devops-project.svc.cluster.local"

  
